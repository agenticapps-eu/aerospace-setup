## Why

Monitor hotplug, native Spaces and independent routing lists caused invisible or misplaced windows. Config atomic writes broke the repository symlink and the build deleted the installed app before compilation.

## What Changes

Implement the requirements below with regression tests, preserve running sessions, and refresh operational documentation.

## Capabilities

### New Capabilities
- `layout-lifecycle`: Reliable operation and recovery.

### Modified Capabilities
None.

## Impact

aerospace-setup and its companion repository; no native Spaces reset. User explicitly requested implementation, checking and pushing. Linear issue: none supplied (local maintenance).
